# Three-Sum-Problem
3Sum Problem : Given an integer array nums, find all unique triplets (a, b, c) such that a + b + c = 0. The solution avoids duplicates and returns all valid combinations efficiently.
